import "./App.css";
import NavBar from "@/scenes/nav-bar";

type Props = {};

const App = (props: Props) => {
  return (
    <div className="app">
      <NavBar />
    </div>
  );
};

export default App;
